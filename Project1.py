# Sydney Honyouti

# Program 1
# This is a python comment
# Create some dynamically typed variables and initialize them
an_integer = 55
a_string = "This is a Python program"
a_bool = False
a_float = 3.25

# Here is a calculation
the_answer = an_integer * a_float + ord(a_string[0]) + int(a_bool)
print(the_answer)

# What about a_string[0] and a-bool
print(a_string[0] + " = " + str(ord(a_string[0])))
print(int(a_bool))

# Write code that will raise an_integer to the fourth power
# Output the_answer to one decimal place
an_integer = pow(an_integer, 4)
print(float(an_integer))
